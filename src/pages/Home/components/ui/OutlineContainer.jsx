// 파일 경로: src/components/ui/OutlineContainer.js

import { Grid, styled } from "@mui/material";

const OutlineContainer = styled(Grid)({
  overflow: "hidden",
  backgroundColor: "#f2f2f2",
});

export default OutlineContainer;
