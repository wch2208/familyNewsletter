import { Grid, styled } from "@mui/material";

const HeroSectionContainer = styled(Grid)({
  overflow: "hidden",
  justifyContent: "center",
  marginBottom: 1,
  objectFit: "cover",
});

export default HeroSectionContainer;
